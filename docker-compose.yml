version: "3.9"

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/backend/data   # persist SQLite sessions
    env_file:
      - .env
    restart: unless-stopped
